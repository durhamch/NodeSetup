<div id="output">
	<h2>Links Visited</h2>
	<ul class="links_visited">
		<!-- Iterate over each element in JSON object -->
		{{each website}}
			<li>{{this.depth}}</li>
		{{else}}
			<span>No content</span>
		{{/each}}
	</ul>
</div>
